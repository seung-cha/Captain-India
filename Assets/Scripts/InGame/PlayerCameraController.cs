using Cinemachine;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerCameraController : MonoBehaviour
{
    // public CinemachineVirtualCamera virtualCam;
   //  CinemachineFramingTransposer tranposer;


    void Start()
    {
     //   tranposer = virtualCam.GetCinemachineComponent<CinemachineFramingTransposer>();
       // tranposer.m_TrackedObjectOffset = new Vector3(-2.5f , 0, 0);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
